// Verifique se TODOS os números do array são positivos
let numeros = [5, 10, 15, 20];

// Use every() para verificar e exiba o resultado no console

let positivos = numeros.every((numero) => numero >= 0);

console.log(positivos);
